// Sample functionality to load booking history dynamically if needed
document.addEventListener('DOMContentLoaded', function() {
    // This could be dynamically loaded via an API or local storage for a real app
    // Example: Fetch booking data from an API and populate the booking history
});
